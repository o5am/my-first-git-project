on:
  push:
    branches:
      - master



jobs:
  bulid:
    runs-on: ubuntu-latest

    steps:
      - name: 'test githup'
        run: pwd 

      - name: 'test print'
        run: echo osama    
